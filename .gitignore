node_modules
.next
out

# Local env files — NEVER commit real keys. Only the *.example templates are tracked.
.env
.env.*
!.env.example

.vercel
*.log
.DS_Store
playwright-report
test-results

# Local screenshots / MCP scratch
/*.png
.playwright-mcp/

# Operator output — real client/business data, keep out of the repo
checks/
tools/check-cli/outreach/
tools/check-cli/run-batch.sh

# Local redesign scratch — already used, not part of the tracked product
redesign/

# Agent skills (marketingskills via `npx skills`). Restore with `npx skills experimental_install`.
# skills-lock.json IS tracked (source of truth); the materialised skills + symlinks are not.
.agents/
.claude/skills/
