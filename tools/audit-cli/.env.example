# AAO Audit CLI — environment.
#
# Required: your Anthropic API key (the pipeline calls Claude).
# https://console.anthropic.com  →  API Keys
# Copy to `.env`, fill in, and load before running (set -a; . ./.env; set +a).
# Never commit your real key. The CLI never logs it.
ANTHROPIC_API_KEY=
